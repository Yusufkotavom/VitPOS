{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Desktop capability for shell, SQL, updater, and process access",
  "windows": ["main"],
  "permissions": [
    "shell:allow-open",
    "sql:default",
    "updater:default",
    "process:default"
  ]
}
